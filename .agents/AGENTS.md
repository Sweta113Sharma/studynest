# Custom Project Rules for StudyNest

## Continuous Version Control Workflow
- **Automatic Git Commits & Pushes**: After completing any code changes requested by the user, automatically stage all modified files (`git add .`), create a descriptive commit message (`git commit -m "..."`), and push to GitHub (`git push origin main`) to ensure all changes are tracked and visible immediately on the repository.
